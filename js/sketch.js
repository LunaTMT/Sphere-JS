// Instantiate the globe sketch in the globe-container
var globe_sketch = new p5(sketch_main, 'globe-container');

// Instantiate the UI sketch in the ui-container
var UI_sketch = new p5(sketch_UI, 'ui-container');


