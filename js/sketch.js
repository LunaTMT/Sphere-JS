var globe_sketch = new p5(sketch_main, 'globe-container');
var UI_sketch    = new p5(sketch_UI,   'ui-container');

/*
// Get the UI container element
var uiContainer = document.getElementById('ui-container');

// Function to update UI container properties
function updateUIContainer() {
  var screenWidth = window.innerWidth;
  console.log("changign");
  uiContainer.style.width = (screenWidth * 0.2) + 'px'; // Set width to 20% of the window width
}

// Call the update function initially
updateUIContainer();

// Add event listener for window resize
window.addEventListener('resize', updateUIContainer);
*/